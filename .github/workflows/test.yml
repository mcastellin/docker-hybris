name: Test

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
      name: Test ybase image build
      run: ybase/build_img.sh test/sap-commerce_testbundle.zip
